spring:
  application:
    name: monitoring-test

management:
  endpoint:
    prometheus:
      enabled: true

  endpoints:
    web:
      exposure:
        include: "*"

  metrics:
    tags:
      application: ${spring.application.name}
